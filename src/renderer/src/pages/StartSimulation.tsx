export default function StartSimulation() {
  return (
    <div style={{ padding: 24 }}>
      <h1 style={{ fontFamily: 'var(--sans-font)', fontSize: 24, fontWeight: 600, color: '#1a1a2e', margin: '0 0 8px 0' }}>
        Start New Simulation
      </h1>
      <p style={{ fontFamily: 'var(--sans-font)', fontSize: 14, color: '#6b6b8a' }}>
        Create a new medical simulation session.
      </p>
    </div>
  )
}
